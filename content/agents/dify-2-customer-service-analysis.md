---
title: "客服对话记录分析工作流"
date: 2026-08-12
description: "基于 Dify 编排的客服对话分析工作流，从表头为 msg_id、history_message 的客服对话 Excel 中提取数据，逐条完成情感分析、问题分类与内容总结，以 JSON 结构化返回。"
summary: "解析客服对话 Excel，逐条做情感分析、问题分类与内容总结，JSON 结构化输出。"
tags: ["Dify", "工作流", "数据分析"]
---

## 项目定位

这是一个基于 Dify 编排的"客服对话记录分析"工作流。它针对**表头为 `msg_id`、`history_message` 的客服对话 Excel 文件**工作：先提取表格中的逐条对话记录，再对每一条完成**情感分析、问题分类、内容总结**，最后以 **JSON 结构化数据**返回分析结果，便于后续做客服质量评估与问题追踪。

## 数据格式

输入为客服对话 Excel，需包含以下列：

- **`msg_id`**：消息唯一标识；
- **`history_message`**：该条消息的对话文本内容（含用户咨询与客服回复）。

工作流对每一行分别处理，不跨行合并。

## 在线体验

> ⚠️ 该工作流目前仅在本机本地部署环境（`localhost`）可用。在本机开发环境中打开本站时 iframe 正常加载；站点发布到公网后，访客访问该地址不会生效，届时需替换为受控代理地址或暂时隐藏体验入口。

<div class="agent-chat-frame">
  <iframe
    src="http://localhost:8091/workflow/zovP7jljPRS5ZRdZ"
    style="width: 100%; height: 100%; min-height: 700px"
    frameborder="0"
    allow="microphone; camera">
  </iframe>
</div>

## 当前能力

- **Excel 提取**：解析表头为 `msg_id`、`history_message` 的客服对话数据；
- **逐条情感分析**：判断每一条消息的情感倾向（如正面、负面、中性）；
- **问题分类**：将对话按问题类型归类，便于定位高频问题；
- **内容总结**：对每条对话做要点归纳，提炼核心诉求；
- **JSON 输出**：以结构化 JSON 返回完整分析结果，便于程序化消费。

## 工程说明

- 基于 Dify 可视化工作流编排数据提取与分析流程；
- 本地部署，保留对数据与应用的控制权；
- 通过 iframe 内嵌到作品集，提供可试用的工作流体验。

## 后续方向

在能稳定、可验证地对外提供访问之前，该体验入口保持本地 / 内网可用。公网访问需要通过反向代理，并做好鉴权与限流。
